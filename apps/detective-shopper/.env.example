IMPACT_API_KEY=
SPOONACULAR_API_KEY=
# Data integrations (see /admin)
UPC_DATABASE_KEY=
COUPON_FEED_API_KEY=
# Optional: tag affiliate clicks by media partner
IMPACT_MEDIA_PARTNER_ID=
# Sign in with Google (public OAuth client id) + session signing secret
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
AUTH_SECRET=
# Durable admin analytics (Redis via Vercel Marketplace / Upstash).
# Auto-injected when you add a Redis store in Vercel; falls back to file/memory.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Optional: protect /admin (leave empty to allow open access in development)
ADMIN_PASSWORD=
